spring:
### 스프링 부트에서 메모리 모드로 동작할때 아래의 설정이 없어도 동작한다.
#
#  datasource:
#    url: jdbc:h2:mem:test # 자바(JVM) 인메모리 DB를 사용하는 방법이다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#
#  jpa:
#    hibernate:
#      ddl-auto: create # default는 create-drop: 종료 시점에 drop해서 메모리 비어준다.
#    properties:
#      hibernate:
##        show_sql: true : System.out 에 의해서 출력
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # 로거를 통해서 출력
    org.hibernate.type: trace # SQL 실행 파라미터 로그 남기기
